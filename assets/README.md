# Assets Directory

## Images

Current images:
- `profile.png` / `profile.jpg` — Your profile photo (used in About section)
- `favicon.png` — Site favicon (appears in browser tab)

### To Update:

1. **Profile Photo** (`profile.jpg` or `profile.png`):
   - Replace with a professional but friendly photo
   - Recommended size: 400x400px minimum
   - Keep file size under 200KB for performance
   - Current path used in HTML: `assets/images/profile.jpg`

2. **Favicon** (`favicon.png`):
   - Replace with your personal brand icon
   - Recommended size: 512x512px (will be scaled automatically)
   - Can be a simple initial, logo, or abstract design
   - Consider using a warm color to match the site theme (orange/clay)
   - Alternative: Add a `favicon.ico` to the root directory

### Optional Additions:

- **Open Graph Image** (`og-image.jpg`):
  - Social media preview image (1200x630px)
  - Shows when sharing the site on Twitter, LinkedIn, etc.
  - Add to HTML: `<meta property="og:image" content="https://richardsd.github.io/assets/images/og-image.jpg">`

## CSS

- `styles.css` — Main stylesheet for the entire site

## JS

- `main.js` — JavaScript for mobile navigation and interactions
